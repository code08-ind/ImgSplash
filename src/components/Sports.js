import React from 'react';
import Common from './Common';

const Sports = () => {
    const type = "sports";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Sports;
