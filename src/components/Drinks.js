import React from 'react';
import Common from './Common';

const Drinks = () => {
    const type = "drinks";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Drinks;
