import React from 'react';
import Common from './Common';

const Arts = () => {
    const type = "arts";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Arts;
