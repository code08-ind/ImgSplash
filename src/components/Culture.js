import React from 'react';
import Common from './Common';

const Culture = () => {
    const type = "culture";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Culture;
