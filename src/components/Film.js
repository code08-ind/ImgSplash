import React from 'react';
import Common from './Common';

const Film = () => {
    const type = "film";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Film;
