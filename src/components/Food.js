import React from 'react';
import Common from './Common';

const Food = () => {
    const type = "food";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Food;
