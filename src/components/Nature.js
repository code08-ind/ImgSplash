import React from 'react';
import Common from './Common';

const Nature = () => {
    const type = "nature";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Nature;
