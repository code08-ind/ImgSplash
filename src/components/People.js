import React from 'react';
import Common from './Common';

const People = () => {
    const type = "people";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default People;
