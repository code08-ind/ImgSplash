import React from 'react';
import Common from './Common';

const Health = () => {
    const type = "health";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Health;
