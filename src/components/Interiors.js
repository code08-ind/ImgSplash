import React from 'react';
import Common from './Common';

const Interiors = () => {
    const type = "interiors";
    return (
        <>
            <Common type={type}/>
        </>
    );
}

export default Interiors;
