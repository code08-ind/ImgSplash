import React from 'react';
import Common from './Common';

const Animals = () => {
    const type = "animals";
    return (
        <>
            <Common type={type} />
        </>
    );
}

export default Animals;
